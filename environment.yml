name: genx3d
channels:
  - conda-forge
  - cadquery
dependencies:
  - python=3.11
  - cadquery
  - pip
  - pip:
    # Poetry will manage these, but we can specify versions here too
    - fastapi>=0.104.0
    - uvicorn[standard]>=0.24.0
    - langchain>=0.1.0
    - langchain-openai>=0.0.5
    - langchain-core>=0.1.0
    - langgraph>=0.0.20
    - sentence-transformers>=2.2.2
    - pinecone>=7.3.0
    - pydantic>=2.5.0
    - jinja2>=3.1.0
    - aiofiles>=23.0.0
    - python-dotenv>=1.0.0